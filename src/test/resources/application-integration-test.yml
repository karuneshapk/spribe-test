spring:

  application:
    name: bookingsystem

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:db/changelog/db.changelog-test.yaml
    enabled: true

app:
  payment:
    expiration-time: 3
    time-unit: SECONDS
